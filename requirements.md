# スタンプラリーアプリ 要件定義書

## 1. 概要

Vercel × Supabase を用いた **ユーザー提示型スタンプラリー Web アプリ** を構築する。
ユーザーが QR コードを提示し、各ポイントの管理者がスキャンすることでスタンプが付与される仕組みとする。

---

## 2. 技術スタック

| 項目 | 技術 |
|------|------|
| フロントエンド | Next.js (App Router) |
| ホスティング | Vercel |
| バックエンド / DB | Supabase (PostgreSQL + Realtime) |
| 認証 | Supabase Auth（ユーザー側） |
| PWA | next-pwa または Service Worker |
| QR コード生成 | qrcode.react 等 |
| QR コードスキャン | html5-qrcode 等 |

---

## 3. 非機能要件

- **PWA 対応**: ホーム画面への追加（アプリインストール）が可能な状態にする。
- **パフォーマンス**: 遅延・タイムアウトが発生しないよう、DB アクセスを最小限に抑えた軽量設計とする。
- **セキュリティ**: アプリ URL が拡散されても、管理者ページはパスコードなしではアクセスできない。
- **スケーラビリティ**: OTP コードの累積によるDB肥大化を防ぐため、期限切れコードは自動削除する。

---

## 4. リポジトリ・デプロイ

- **GitHub**: `https://github.com/agcelerain-glitch/easy-stamp-apps.git`
- **デプロイ先**: Vercel（GitHub 連携は手動で設定）
- **注意**: `other/` ディレクトリは `.gitignore` に含め、リポジトリにプッシュしない。

---

## 5. 画面構成

```
/ (トップページ)
├── /user         ← ユーザー画面（ログイン → スタンプ画面）
│   └── /user/stamps  ← スタンプ一覧・QR 発行画面
└── /admin        ← 管理者入口（パスコード入力）
    ├── /admin/1  ← スタンプポイント 1 管理画面
    ├── /admin/2  ← スタンプポイント 2 管理画面
    ├── /admin/3  ← スタンプポイント 3 管理画面
    ├── /admin/4  ← スタンプポイント 4 管理画面
    └── /admin/5  ← スタンプポイント 5 管理画面
```

---

## 6. トップページ (`/`)

- **「スタンプラリーを始める」** ボタン → `/user` へ遷移（ユーザー向け、目立つデザイン）
- **「管理者の方はこちら」** テキストリンク → `/admin` へ遷移（控えめなデザイン）

---

## 7. ユーザー機能

### 7-1. ニックネーム入力・匿名セッション開始

- **認証方式**: Supabase 匿名認証（Anonymous Auth）を使用。メールアドレス・パスワード不要。
- ニックネームを入力してスタートする。
- ニックネームは `profiles` テーブルで **ユニーク** に管理する。
- **新規ニックネーム**: 匿名ユーザーを作成 → `profiles` レコード生成 → スタンプ画面へ。
- **既存ニックネーム**: 「続きからはじめる」として同一プロフィールに紐付け、過去のスタンプ状況を復元する。
- ブラウザセッション消失時でも、ニックネームで DB からスタンプ進捗を参照・復元できる。

### 7-2. スタンプ一覧画面 (`/user/stamps`)

- スタンプ枠を 5 つ表示する。
- 各スタンプの取得済み / 未取得状態をビジュアルで示す。
- 取得済みのスタンプは絵柄が表示され、未取得は空枠とする。
- 全 5 つ取得でコンプリート演出（任意）。

### 7-3. OTP QR コード発行

- 未取得のスタンプ枠をタップ → **そのポイント用 OTP QR コードを生成・表示**。
- QR コードには `{ token: UUID, stamp_point_id: 1~5, user_id, expires_at }` に相当する情報を含む。
- 発行と同時に Supabase の `otp_codes` テーブルに保存する。
- **有効期限: 発行から 1 時間**。
- 有効期限はカウントダウン表示（任意）。
- 1 つのスタンプポイントにつき同時に複数の有効 OTP を発行しない（既存コードがあれば再表示または再発行の選択）。

---

## 8. 管理者機能

### 8-1. 管理者入口 (`/admin`)

- パスコード入力フォームを表示する。
- 入力されたパスコードに対応するスタンプポイント番号（1〜5）を判定し、`/admin/{n}` へリダイレクトする。
- パスコードは各ポイントで異なる 5 種類を用意する（環境変数で管理）。
- 不正入力はエラー表示のみ（ロック機能は任意）。

### 8-2. 管理者スキャン画面 (`/admin/{n}`)

- カメラを起動し、QR コードをスキャンする。
- スキャン後、Supabase API に対してトークンの検証リクエストを送る。
  - `stamp_point_id` がそのページの番号と一致するか確認。
  - `expires_at` が現在時刻以内か確認。
  - 対象ユーザーの当該スタンプが未取得か確認。
- 検証成功 → ユーザーの `stamps` テーブルを更新（スタンプ付与）し、`otp_codes` から該当レコードを削除。
- 検証失敗（期限切れ / 不正 / 対象外ポイント）→ エラーメッセージを表示。
- スキャン画面はシンプルかつ高速に動作させる。

---

## 9. データベース設計（Supabase）

### `profiles` テーブル

| カラム | 型 | 説明 |
|--------|-----|------|
| id | uuid (PK, FK → auth.users) | ユーザー ID |
| nickname | text | ニックネーム |
| created_at | timestamptz | 作成日時 |

### `stamps` テーブル

| カラム | 型 | 説明 |
|--------|-----|------|
| id | uuid (PK) | レコード ID |
| user_id | uuid (FK → profiles) | ユーザー ID |
| stamp_point_id | int (1〜5) | スタンプポイント番号 |
| stamped_at | timestamptz | スタンプ付与日時 |

- `(user_id, stamp_point_id)` にユニーク制約。

### `otp_codes` テーブル

| カラム | 型 | 説明 |
|--------|-----|------|
| id | uuid (PK) | OTP トークン ID |
| user_id | uuid (FK → profiles) | 発行ユーザー ID |
| stamp_point_id | int (1〜5) | 対象スタンプポイント |
| expires_at | timestamptz | 有効期限（発行 +1h） |
| created_at | timestamptz | 発行日時 |

### 自動削除の仕組み

- Supabase の **pg_cron** または **Edge Functions** を用いて、`expires_at < now()` のレコードを定期削除する。
- 管理者スキャン成功時にも即時削除する。

---

## 10. セキュリティ設計

- **管理者パスコード**: 環境変数（`ADMIN_PASS_1` 〜 `ADMIN_PASS_5`）に保存し、フロントエンドには露出しない。パスコード照合は Supabase Edge Function または Next.js API Route で行う。
- **Row Level Security (RLS)**: Supabase RLS を設定し、ユーザーは自身のデータのみ参照・更新可能とする。
- **OTP 検証**: Edge Function または API Route で行い、クライアントから直接 DB を書き換えさせない。

---

## 11. PWA 設定

- `manifest.json` を設置し、アプリ名・アイコン・テーマカラーを定義する。
- Service Worker によるオフラインキャッシュ（最低限のシェルキャッシュ）を設定する。
- iOS / Android 両対応のホーム画面追加を可能とする。

---

## 12. 開発フェーズ（目安）

1. プロジェクト初期設定（Next.js + Supabase 連携、PWA 設定）
2. DB スキーマ作成・RLS 設定
3. ユーザー認証・ニックネーム設定
4. スタンプ画面・OTP QR 発行機能
5. 管理者パスコード認証・スキャン画面
6. OTP 自動削除（pg_cron / Edge Function）
7. デザイン調整・PWA 動作確認
8. Vercel デプロイ・本番動作確認
